server:
  port: 8080

spring:
  application:
    name: g8-analytics-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/g8_analytics}
      database: g8_analytics

logging:
  level:
    ma.sgitu.g8: DEBUG
